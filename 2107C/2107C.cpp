/// hhoangcpascal

#include <iostream>
#include <algorithm>
#include <climits>

using namespace std;
long double A, B;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    cin >> A >> B;
    cout << (long long)((A + B) / 2.0);

    return 0;
}
